# dropping existing tables method 1
# open python shell
>>> from app import db
>>> db.drop_all()
>>> db.session.commit()

# dropping existing tables method 2
# open psql shell
>>> \c db_name
# drop the database
>>> DROP DATABASE db_name
# create the database
>>> CREATE DATABASE db_name

# adding new schemas to the db
# open python shell
>>> from app import db 
>>> db.create_all()
>>> db.session.commit()